name: r5-workflow-audit

on:
  push:
    branches: [ main ]
    paths:
      - ".github/workflows/*.yml"
      - ".github/workflows/*.yaml"
      - "**/*.yml"
      - "**/*.yaml"
  pull_request:
    branches: [ main ]
    paths:
      - ".github/workflows/*.yml"
      - ".github/workflows/*.yaml"
      - "**/*.yml"
      - "**/*.yaml"
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # GitHub Actions 工作流程語法檢查（官方 action）
      - name: actionlint (GitHub Actions linter)
        uses: rhysd/actionlint@v1
        with:
          # 可固定版本，避免上游突變
          version: "1.7.3"

      # YAML 靜態檢查
      - name: yamllint
        uses: ibiqlik/action-yamllint@v3
        with:
          # 將輸出轉成 GitHub annotations；若有 .yamllint 會自動讀取
          format: github
          strict: true
